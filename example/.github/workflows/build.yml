# Generated by github-actions-cdk. To modify, edit workflowsrc.ts and run "ts-node workflowsrc.ts"

name: build
on:
  push:
    branches:
      - main
  pull_request: {}
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CI: "true"
    steps:
      - id: checkout
        uses: actions/checkout@v4
