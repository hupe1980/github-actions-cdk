# Generated by github-actions-cdk

name: Build
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CI: "true"
    steps:
      - id: checkout
        name: Checkout Code
        uses: actions/checkout@v4
      - id: setup-node
        name: Set up Node.js
        uses: actions/setup-node@v4
